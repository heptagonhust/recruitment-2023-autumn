#include "conway.hh"
#include <iostream>
#include <fstream>
#include <limits>
#include <random>

using namespace std;

int main(int argc, char *argv[]) {
    if (argc != 4) {
        cout << "Usage: ./generator <vertex_num> <raw_filename> <check_filename>" << endl;
        return 0;
    }
    return 0;
}